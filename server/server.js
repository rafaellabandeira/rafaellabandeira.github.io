Aquí tienes el `server.js` completo. El único cambio respecto al original es el endpoint `/admin/verificar` al final, antes del `app.listen`.

**Resumen de pasos para desplegar:**
1. Reemplaza tu `server.js` con este
2. Reemplaza tu `main.js` con el de antes
3. En Render → Environment → añade `ADMIN_PASSWORD = tu_contraseña`
4. Redespliega

¡Y listo! 🎉
